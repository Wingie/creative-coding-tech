lang: en
timezone: Europe/Amsterdam
future: true

title: Creative Coding Tech
tagline: "Engineering Leader. Creative Technologist. AI/ML Architect."

# Site logo
avatar: /logo.png
description: >-
  Wingston Sharon Wilson — Engineering Manager @ Booking.com. 15+ years
  building AI/ML systems, distributed platforms, and creative technology.
  Amsterdam-based founder and live coding artist.

url: "https://creativecodingtech.com"

github:
  username: wingie

twitter:
  username: creativecodingtech
  card: summary_large_image

linkedin:
  username: wingstonsharon

social:
  name: Wingston Sharon Wilson
  email: creativecodingtech@proton.me
  links:
    - https://github.com/wingie
    - https://linkedin.com/in/wingstonsharon

author: Wingston Sharon Wilson

# Google Search Console verification (replace with actual code)
# google_site_verification: "your-verification-code"

theme: jekyll-theme-chirpy

# Exclude from processing
exclude:
  - ppts

# Chirpy theme settings
# NOTE: `paginate` is intentionally unset. jekyll-paginate only paginates the
# site-root index.html, which here is the landing page — so enabling it produced
# /page2..5/ as duplicate copies of the landing page (all indexed in sitemap.xml)
# while /blog/ still received no paginator. _layouts/home.html lists all posts.

# Collections — required for _tabs/ to output pages
collections:
  tabs:
    output: true
    sort_by: order
  projects:
    output: true
    sort_by: order

defaults:
  - scope:
      path: ""
      type: tabs
    values:
      layout: page
      permalink: /:title/
  - scope:
      path: ""
      type: projects
    values:
      layout: project-detail
      permalink: /projects/:name/

# Jekyll Archives
jekyll-archives:
  enabled: 
    - categories
    - tags
  layouts:
    category: category
    tag: tag
  permalinks:
    category: /categories/:name/
    tag: /tags/:name/